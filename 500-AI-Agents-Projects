import java.util.*;
class Main{
  public static void main(String[] args) {
    Scanner sc = new Scanner(System.in);
    List<int[]> adj = new ArrayList<>();
    int n = sc.nextInt();
    for(int i=0;i<n;i++){
      int u = sc.nextInt();
      int v = sc.nextInt();
      int w = sc.nextInt();
      adj.add(new int[]{u,v});
    }
  }
}